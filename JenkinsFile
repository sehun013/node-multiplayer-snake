
    node {
        stage('SCM') {
            checkout scm 
        }
        stage('Scanner') {
            def scannerHome = tool 'Scanner';
            withSonarQubeEnv('SonarServer') { 
                sh "${scannerHome}/bin/sonar-scanner"
            }
        }
    }